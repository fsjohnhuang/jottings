## BINARY和ASCII传输模式
**BINARY模式**<Br/>
复制时保留文件的位序，逐位复制文件。<Br/>
**ASCII模式**<Br/>
复制时会对文件内容进行调整，主要体现在回车、换行和结束符等的转译。<br/>
回车符在Unix下是\n(0A)，Windows下是\r\n(0D0A)，Mac下是\r(0D)。因此从Unix上下载文件到Windows时，会将\n转译为\r\n，那么对于文本文件来说就很好了。但对于二进制文件就会导致文件失效。<br/>
